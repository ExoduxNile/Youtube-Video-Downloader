import React from 'react';

export default function Layout({ children }) {
  return (
    <>
      <header>
        // navigation
      </header>
      {children}
      <footer>
        // footer content
      </footer>
    </>
  )
}